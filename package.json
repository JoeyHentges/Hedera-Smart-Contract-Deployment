{
  "name": "hedera-deploy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "set \"NODE_ENV=development\" && npm run build && node dist/src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hashgraph/sdk": "^2.5.0",
    "dotenv": "^10.0.0",
    "typescript": "^4.5.2"
  }
}
